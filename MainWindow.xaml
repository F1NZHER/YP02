<Window x:Class="ElectricityApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Учёт оплаты электроэнергии" 
        Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Учёт оплаты электроэнергии" 
                   FontSize="20" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,0,0,10">

            <Button Name="btnAddAbonent" 
                    Content="Добавить абонента" 
                    Click="btnAddAbonent_Click"
                    Width="130" Height="35" Margin="5"
                    Background="#4CAF50" Foreground="White"
                    BorderThickness="0"/>

            <Button Name="btnAddPayment" 
                    Content="Добавить платёж" 
                    Click="btnAddPayment_Click"
                    Width="130" Height="35" Margin="5"
                    Background="#2196F3" Foreground="White"
                    BorderThickness="0"/>

            <Button Name="btnDebtors" 
                    Content="Список должников" 
                    Click="btnDebtors_Click"
                    Width="130" Height="35" Margin="5"
                    Background="#FF9800" Foreground="White"
                    BorderThickness="0"/>

            <Button Name="btnRefresh" 
                    Content="Обновить" 
                    Click="btnRefresh_Click"
                    Width="130" Height="35" Margin="5"
                    Background="#9C27B0" Foreground="White"
                    BorderThickness="0"/>

            <Button Name="btnTestData" 
                    Content="Тест данные" 
                    Click="btnTestData_Click"
                    Width="130" Height="35" Margin="5"
                    Background="#607D8B" Foreground="White"
                    BorderThickness="0"/>
        </StackPanel>

        <!-- Таблица с абонентами -->
        <GroupBox Grid.Row="2" Header="Список абонентов" Margin="0,0,0,10">
            <DataGrid Name="dgAbonents"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Margin="5">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="№" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="150"/>
                    <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="200"/>
                    <DataGridTextColumn Header="Последняя оплата" 
                    Binding="{Binding LastPaymentDate, StringFormat={}{0:dd.MM.yyyy}}" 
                    Width="120"/>
                    <DataGridTextColumn Header="Платежей" 
                                        Binding="{Binding Payments.Count}" 
                                        Width="70"/>
                    <DataGridCheckBoxColumn Header="Должник" 
                                            Binding="{Binding HasDebt, Mode=OneWay}" 
                                            IsReadOnly="True"
                                            Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Статусная строка -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock Name="tbStatus" Text="Готов к работе"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Name="tbCount"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>